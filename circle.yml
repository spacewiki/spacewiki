dependencies:
  post:
    - pip install ansible
test:
  pre:
    - ./app.py --syncdb
  override:
    - nosetests
deployment:
  ratchet:
    branch: master
    commands:
      - ansible-playbook -i deploy/hosts.txt deploy/ratchet.yml -u spacewiki-deploy
