general:
  artifacts:
    - linter.html
dependencies:
  post:
    - pip install ansible pylint
test:
  pre:
    - mkdir uploads
    - ./app.py --syncdb
  override:
    - nosetests
  post:
    - pylint -f html *.py | tee linter.html
deployment:
  ratchet:
    branch: master
    commands:
      - ansible-playbook -i deploy/hosts.txt deploy/ratchet.yml -u spacewiki-deploy
